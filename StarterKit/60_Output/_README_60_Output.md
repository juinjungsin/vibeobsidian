# 60_Output — 최종 결과물

## 역할
Project(50_Project)에서 완성된 산출물의 **최종 보관소**.
배포·공유 가능한 완성본(PPT, DOCX, PDF, 이미지 등)만 저장한다.

## 규칙
- 초안·작업 중 파일은 저장하지 않는다 (`50_Project`에서 작업)
- 파일명에 날짜를 포함한다: `[유형]_[프로젝트명]_[YYYY-MM-DD].[확장자]`
- AI는 사용자의 명시적 지시 없이 이 폴더에 파일을 생성하지 않는다
- `50_Project` 원본은 삭제하지 않는다 (복사만)
